import { Box, Grid } from "@yamada-ui/react";
import PerformanceCardList from "./_components/performanceCardList";
const page = () => {
  return (
    <Box mx={3} m={"auto"}>
      <PerformanceCardList />
    </Box>
  );
};

export default page;
